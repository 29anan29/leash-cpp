// 提示词与对话包（基于 ai 原生包）
// 导入后可直接调用：prompt_build / prompt_chat

package ai

fn prompt_build(system: str, user: str) -> str
    return "【system】" + system + "\n【user】" + user

fn prompt_chat(system: str, user: str) -> str
    return chat(system, user)
