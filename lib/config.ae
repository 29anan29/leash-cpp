// 配置加载包（基于 file 与 json 原生包）
// 导入后可直接调用：config_load / config_get

package file
package json

fn config_load(path: str) requires file
    return parse(read(path))

fn config_get(path: str, key: str) requires file
    let data = parse(read(path))
    return json_get(data, key)
