// 哈希扩展包（基于 crypto 与 file 原生包）
// 导入后可直接调用：hashx_sha256 / hashx_file

package crypto
package file

fn hashx_sha256(s: str) -> str
    return sha256(s)

fn hashx_file(path: str) -> str
    return sha256(read(path))
