// 取首字母缩写
import "string"

fn acronym(words: str)
    let parts = str_split(str_upper(words), " ")
    let mut buf = ""
    let mut i = 0
    while i < len(parts)
        let w = parts[i]
        if len(w) > 0
            buf = buf + w[0:1]
        i = i + 1
    return buf
