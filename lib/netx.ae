// 网络扩展包（基于 http 原生包）
// 导入后可直接调用：netx_get / netx_post

package http

fn netx_get(url: str) -> str requires net
    return get(url)

fn netx_post(url: str, body: str) -> str requires net
    return post(url, body)
